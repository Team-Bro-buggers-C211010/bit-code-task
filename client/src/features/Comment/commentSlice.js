import { createSlice } from '@reduxjs/toolkit';
const initialState = {};

const commentSlice = createSlice(
    {
        name: "comment",
        initialState,
        reducers: {},
        extraReducers: () => {}
    }
);

export default commentSlice;
